name: Ad Service CI

on:
  push:
    branches: [main]
    paths:
      - "src/ad-service/**"
      - ".github/workflows/ad-service-ci.yml"
      - "VERSION"
  pull_request:
    branches: [main]
    paths:
      - "src/ad-service/**"
      - ".github/workflows/ad-service-ci.yml"

env:
  JAVA_VERSION: "19"

jobs:
  build-test-push:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src/ad-service

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image (includes tests)
        run: docker build -t ad-service:latest .

      - name: Read version
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        id: version
        run: |
          VERSION=$(cat ../../VERSION)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - name: Login to Docker Hub
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Tag and push
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          docker tag ad-service:latest ${{ secrets.DOCKERHUB_USERNAME }}/ad-service:${{ steps.version.outputs.VERSION }}
          docker tag ad-service:latest ${{ secrets.DOCKERHUB_USERNAME }}/ad-service:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/ad-service:${{ steps.version.outputs.VERSION }}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/ad-service:latest
